<%= form_with(model: blogs_title, local: true) do |form| %>
  <% if blogs_title.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blogs_title.errors.count, "error") %> prohibited this blogs_title from being saved:</h2>

      <ul>
      <% blogs_title.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
